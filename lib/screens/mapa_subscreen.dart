import 'package:flutter/material.dart';

class MapaSubscreen extends StatelessWidget {
   
  const MapaSubscreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Center(
         child: Text('MapaSubscreen'),
      );
    
  }
}