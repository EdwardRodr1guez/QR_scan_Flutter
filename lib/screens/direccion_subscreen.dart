import 'package:flutter/material.dart';

class DireccionSubscreen extends StatelessWidget {
   
  const DireccionSubscreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Center(
         child: Text('DireccionSubscreen'),
      );
    
  }
}